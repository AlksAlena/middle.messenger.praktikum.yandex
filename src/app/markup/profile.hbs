---
title: Настройки пользователя | Zoo-Chat
pageHeader: Настройки пользователя
fields:
  - label: Логин
    inputName: login
    fieldType: text
  - label: Пароль
    inputName: password
    fieldType: password
submitButtonText: Сохранить
---

{{#extend "base"}}
  {{#content "main"}}
    <main>
      <div class="content">
        <form class="form" action="">
          <h1>{{pageHeader}}</h1>

          <ul class="form-fields">
            {{#each fields}}
              {{#and label inputName fieldType}}
                <li class="form-field">
                  <label class="label" for="{{inputName}}">{{label}}</label>
                  <input class="input" name="{{inputName}}" type="{{fieldType}}">
                </li>
              {{/and}}
            {{/each}}
          </ul>

          {{> submit-button}}
        </form>
      </div>
    </main>
  {{/content}}
{{/extend}}